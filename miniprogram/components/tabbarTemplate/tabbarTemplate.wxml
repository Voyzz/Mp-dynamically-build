<van-tabbar active="{{ active }}" bind:change="onChange">
  <block wx:for="{{gridArr}}" wx:key="{{index}}">
    <van-tabbar-item icon="{{item.icon}}">{{item.text}}</van-tabbar-item>
  </block>
</van-tabbar>