<!--pages/homepage/homepage.wxml-->
<view>
    <view wx:for="{{renderModulesInfo}}">
        <titleTemplate wx:if="{{item.type === 1}}" title="{{item.title}}" my-class="titleModule"></titleTemplate>
    </view>
</view>
