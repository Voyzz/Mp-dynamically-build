<!--pages/homepage/homepage.wxml-->
<view>
    <view wx:for="{{renderModulesInfo}}">
        <titleTemplate wx:if="{{item.type === 1}}" title="{{item.title}}" my-class="titleModule">
        </titleTemplate>
    </view>
    <van-button type="primary">按钮</van-button>
</view>
