<!--pages/homepage/homepage.wxml-->
<view>
    <view wx:for="{{renderModulesInfo}}">
        <titleTemplate wx:if="{{item.type === 1}}" title="{{item.title}}" my-class="titleModule" class="page">
          <view class="weui-footer">我是页脚</view>
        </titleTemplate>
    </view>
</view>
